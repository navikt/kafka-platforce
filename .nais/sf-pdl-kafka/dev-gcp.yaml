spec:
  gcp:
    sqlInstances:
      - type: POSTGRES_17
        tier: db-f1-micro
        databases:
          - name: sf-pdl-kafka-dev # Follow name convention <app-name>-<context>
  ingresses:
    - "https://sf-pdl-kafka.intern.dev.nav.no"
  envFrom:
    - secret: sf-pdl-kafka
  env:
    - name: KAFKA_TOPIC
      value: pdl.pdl-persondokument-tagged-v1
    - name: WHITELIST_FILE
      value: '/whitelist/dev.json'